<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pesquisa="clr-namespace:Xamarin.HLP.Mobile.AppPE.ViewModel.Pesquisa;assembly=Xamarin.HLP.Mobile.AppPE"
             x:Class="Xamarin.HLP.Mobile.AppPE.View.Pesquisas.PageBasicList">
    <ContentPage.BindingContext>
        <pesquisa:BasicPesquisaViewModel/>
    </ContentPage.BindingContext>
    <ListView ItemsSource="{Binding Itens}" SelectedItem="{Binding currentItemModel}" ItemSelected="ListView_OnItemSelected" CachingStrategy="RecycleElement">
        <!--<ListViewCachingStrategy>RecycleElement</ListViewCachingStrategy>-->
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Display }" Detail="{Binding Detail}" TextColor="{StaticResource CinzaMaisEscuro}" DetailColor="{StaticResource CinzaClaro}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>