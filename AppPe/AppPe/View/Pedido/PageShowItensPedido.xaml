<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             xmlns:pedido="clr-namespace:Xamarin.HLP.Mobile.AppPE.ViewModel.Pedido;assembly=Xamarin.HLP.Mobile.AppPE"
             xmlns:pedido1="clr-namespace:Xamarin.HLP.Mobile.AppPE.View.Converter.Pedido;assembly=Xamarin.HLP.Mobile.AppPE"
             xmlns:abstractions="clr-namespace:Xamarin.HLP.Mobile.AppPE.Abstractions;assembly=Xamarin.HLP.Mobile.AppPE"
             x:Class="Xamarin.HLP.Mobile.AppPE.View.Pedido.PageShowItensPedido"
             Title="Itens do pedido" Padding="10,25,10,10">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
      PositionIn="Center"
      PositionOut="Center"
      ScaleIn="1.2"
      ScaleOut="0.8"
      DurationIn="400"
      DurationOut="300"
      EasingIn="SinOut"
      EasingOut="SinIn"
      HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>

    <pages:PopupPage.BindingContext>
        <pedido:ShowItensPedidoViewModel/>
    </pages:PopupPage.BindingContext>

    <pages:PopupPage.Resources>
        <ResourceDictionary>
            <pedido1:DisplayToCountConverter x:Key="DisplayToCountConverter"/>
        </ResourceDictionary>
    </pages:PopupPage.Resources>

    <Frame BackgroundColor="White"  Padding="0,0,0,0"  VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"  OutlineColor="{StaticResource CinzaPrincipal}">
        <Frame.HasShadow>
            <OnPlatform x:TypeArguments="x:Boolean" Android="True" iOS="True" WinPhone="False"/>
        </Frame.HasShadow>

        <Grid Margin="5,0,5,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid VerticalOptions="Center" HorizontalOptions="End"  >
                <Grid.Margin>
                    <OnPlatform x:TypeArguments="Thickness" WinPhone="20,-27,0,0" Android="0,2,2,0"/>
                </Grid.Margin>
                <abstractions:SvgImage Command="{Binding CloseCommand}" SvgPath="{StaticResource ImagemErrorPopup}" HeightRequest="25" WidthRequest="25"/>
            </Grid>
            <ListView Grid.Row="1" ItemsSource="{Binding currentModel.lItens}" CachingStrategy="RecycleElement"
                                       HasUnevenRows="True">
                <!--<ListViewCachingStrategy>RecycleElement</ListViewCachingStrategy>-->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell x:Name="TextCellItem" Text="{Binding xDescricao }" 
                                  TextColor="{Binding ItemJaIncluso, Converter={StaticResource BoolToColorConverterLitaItensPedido}}" Detail="{Binding xDetalheItem}"
                                  DetailColor="{StaticResource VerdePrincipal}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Label Grid.Row="2" Margin="5,5,5,5" Text="{Binding CountItens, Converter={StaticResource DisplayToCountConverter}, ConverterParameter='Total de itens ( {0} )'}" LineBreakMode="NoWrap" FontAttributes="Bold" TextColor="{StaticResource CinzaPrincipal}"/>
        </Grid>
        
    </Frame>
    
    
</pages:PopupPage>