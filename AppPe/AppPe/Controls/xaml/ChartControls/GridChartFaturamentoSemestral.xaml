<?xml version="1.0" encoding="utf-8" ?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:abstractions="clr-namespace:Xamarin.HLP.Mobile.AppPE.Abstractions;assembly=Xamarin.HLP.Mobile.AppPE"
             xmlns:chart="clr-namespace:Xamarin.HLP.Mobile.AppPE.ViewModel.Chart;assembly=Xamarin.HLP.Mobile.AppPE"
             x:Class="Xamarin.HLP.Mobile.AppPE.Controls.xaml.ChartControls.GridChartFaturamentoSemestral"  RowSpacing="0" Margin="0">
  <Grid.BindingContext>
    <chart:FaturamentoSemestralChartViewModel Title="Vendas semestral"/>
  </Grid.BindingContext>
  <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
  </Grid.RowDefinitions>

  <StackLayout Orientation="Vertical" Spacing="0">
    <Grid BackgroundColor="{Binding currentModel.CorTemplate}" Padding="0,7,0,7">
      <Label Text="{Binding Title}" TextColor="White" HorizontalTextAlignment="Center" Style="{StaticResource Medium}" FontAttributes="Bold"/>
    </Grid>
  </StackLayout>

  <StackLayout x:Name="ChartStack" BackgroundColor="{StaticResource Branco}" Grid.Row="1" Orientation="Vertical" Spacing="0" Padding="0,7,0,4">

  </StackLayout>
  
  <Grid HeightRequest="50"  IsVisible="{Binding IsBusy}" Grid.Row="2" >
    <ActivityIndicator  IsRunning="True" />
  </Grid>



</Grid>

